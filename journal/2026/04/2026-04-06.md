# Research Journal — 2026-04-06

## ARAP Bake on A6000 Server

### Headless ARAP Bake
- Used existing `tools/bake_headless.py` with `tools/muscles_L_UpLeg.json` / `tools/muscles_R_UpLeg.json`
- Submitted via slurm: `sbatch tools/slurm_bake_walk_upleg.sh` (array job, 2 GPUs)
- Both L+R completed in ~65s per side
- First run had vertex count mismatch (server had older tet files) — synced local tets to server, re-baked

### Phase 2 IPC: ARAP Rest Shape + Contact Resolution

**Approach**: For each frame, take ARAP-baked positions as rest shape, run pyuipc with IPC contact + SPC on cap vertices only. No SPC-vs-IPC fight because SPC only on caps.

**Validation** (3 muscles — Sartorius, Gracilis, Semitendinosus):
- Converged in 1-5 Newton iterations per step
- Volume ratio 1.0000 (zero shrinkage)
- Cap displacement 0.000mm (perfect attachment)
- Zero inversions

**Full bake** (50 muscles × 132 frames):
- ~3-4s per frame on A6000 GPU
- `bake_ipc_phase2.py` outputs full vertex arrays (unmapped vertices via bary interpolation)
- Writes viewer cache chunks directly (no converter needed)

### Bake Pipeline Fix
- `bake_ipc_phase2.py`: expand unmapped vertices via barycentric interpolation from nearest tet
- Relaxed bary tolerance to -2.0 for surface vertices far from tet centers
- Fixes L/R_Obturator_Externus: 5→0 stuck vertices

### Waypoint Fixes
- `_motion_patch_waypoints`: temporarily disables fix_x/fix_z/fix_rotation during recomputation
- Live waypoint recompute fallback when cache has no waypoints
- Always clamp negative bary coords (prevents opposite-direction overshoot)
- Cleared cached bary coords from 54 tet files to force recomputation

### Contour-Guided Tet Improvements
- Multi-center for thin cross-sections (aspect > 3): polylabel on polygon strips
- Guaranteed interior via polygon slicing + polylabel per strip
