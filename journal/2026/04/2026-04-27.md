# Research Journal — 2026-04-27

## Vectorize ARAP + post-ARAP collision + cache management

### Vectorize ARAP solver, fix constraint enforcement (`87c1c11`)
- Batched local/global ARAP steps in `MuscleSimulation`. Built flat directed-edge arrays in `init_arap`, replaced per-vertex Python loops with `np.add.at` scatter + batched `np.linalg.svd`.
- `solve_arap_muscle` builds target rest as flat `(E_dir, 3)` array, eliminating per-iter Python loop over edges.
- `_enforce_inter_muscle_constraints` averages corrections per vertex (was summing → diverged on dense 1.2M-constraint original-mesh tets, pushed positions to ~1e8 m).
- Initialize `max_disp` before mass-spring loop (avoids `UnboundLocalError` when `phase1_iters=0`).
- `bake_headless.py`: forward `outer-iters` to `run_all_tet_sim_with_constraints`, add `--phase1-iters` / `--outer-iters` / `--no-self-collision` knobs (defaults: original=8/3, contour=50/20).

Result on walk f60-70 × 25 L_UpLeg muscles: ~80 s/frame → ~12 s/frame, no quality regression. Total bake 11 frames: 2m23s.

### Filter motion cache by tet vertex count on BVH (re)load (`09cffcc`)
Cache chunks with mismatched vert count (from previous tet pipeline run) loaded but produced wrong viz. Now filtered out at load.

### Fix mesh swap state detection and cache reload (`52ae4c8`)
Mesh swap was reading stale state when toggling original ↔ contour. Reload triggered properly.

### Order cache chunks by mtime so newest bake wins on frame overlap (`5839393`)
Multiple chunks could cover same frame range (re-bake). Newest wins now.

### Post-ARAP bone collision for contour-mesh muscle sim (`2a1b3d6`)
- New helper `viewer/bone_surface_collision.py:resolve_bone_collisions` — vert-bone (`bone_mesh.contains` + closest-point) + edge-bone ray-cast tunneling resolution.
- Run once after ARAP converges (`muscle_mesh.py:1083`) so push survives elastic step. Uses tet boundary surface, cached per muscle.
- In-loop `_resolve_collisions_arap` disabled by default (ARAP undoes its push, trimesh queries dominate runtime). Restorable via `mobj.soft_body._inloop_collision = True`.
- Bone trimeshes built ONCE per frame in `run_all_tet_sim_with_constraints` and shared across muscles (was hundreds of trimesh constructions per frame).

walk f60-70 × 25 L_UpLeg muscles on a6000: 110 s/frame with bone collision active vs 1.7 s/frame without (unified-volume).
