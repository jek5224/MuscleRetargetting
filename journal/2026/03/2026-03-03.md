# Research Journal — 2026-03-03

## Work Done
- **DistillNet V2**: Designed and implemented V2 architecture -- single decoder with muscle embedding, PCA output targets, sliding window input (W=5), temporal loss, derivative features, and z-score normalization
- **Training Pipeline**: Rewrote training loop for V2 with PCA targets, temporal frame pairs, cosine decay schedule, dropout, input noise augmentation, and increased weight decay
- **Viewer Integration**: Updated viewer for V2 with sliding window input construction, version display, combined checkpoint loading, and CPU-forced inference
- **Headless Baking**: Built headless FEM baking script for server-side simulation with Taichi ARAP backend, waypoint patching, auto BVH rest pose detection, and batch processing with .done markers
- **Waypoint System**: Fixed waypoint NN update to use tet barycentric coords (not KDTree IDW), added fast waypoint update using cached MVC weights
- **Performance**: Fixed OOM issues in decoder batching, checkpoint loading, and combined dataset handling; reduced checkpoint disk usage

## Key Decisions
- Moved from per-muscle decoders to single shared decoder with learned muscle embeddings (V2)
- PCA-space targets instead of raw vertex displacements for better generalization
- Sliding window replaces velocity input for temporal context
