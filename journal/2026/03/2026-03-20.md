# Research Journal — 2026-03-20

## VBD Full Bake Completed (walk.bvh)

### OOM Fix
Previous bake attempt was OOM-killed at frame ~99 (13.2GB RSS). Root cause: `FLUSH_INTERVAL=1000` meant no incremental saves for 132-frame bake — all data accumulated in memory.

**Fix**: `FLUSH_INTERVAL=20` + `gc.collect()` after each flush. Committed as `2a943d7`.

### Bake Results
- **132 frames** in 10,031s (2.8 hours), **75.9s/frame**
- **518 chunk files** (74 muscles × 7 chunks each)
- **74 muscles** with waypoints patched
- Output: `data/motion_cache/walk/`
- VBD solver with collision interleaving (last 2 sub-steps + final pass)
- Convergence: `||dx||` follows walk cycle pattern (1.76 → 1.08 → 1.94 → 1.31)

### Pending
- Visual verification needed: does collision interleaving fix soleus/gastrocnemius penetrating tibia?
- Memory still grows to ~6GB RSS over the bake (trimesh/KDTree accumulation despite gc.collect)
