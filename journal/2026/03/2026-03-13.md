# Research Journal — 2026-03-13

## Dance V1Dec Training — Completed
- Fixed training instability (Adam optimizer loss spikes) with spike guard: detect 10× loss jump → revert to best checkpoint + halve LR
- Replaced CosineAnnealingLR with ReduceLROnPlateau (factor=0.5, patience=200)
- Added optimizer state saving/loading in checkpoints
- Final result: epoch 8258, MSE=1.36e-7, LR=3.75e-5 (halved twice from 3e-4)
- Checkpoint synced locally, works well in viewer for dance.bvh

## Error Heatmap fix_rotation Bug Fix
- When fix_rotation enabled, all muscles appeared red (max error)
- Root cause: heatmap was using already-fixed skeleton transform → R_delta = identity
- Fix: temporarily restore baked pose to get unfixed transform, compute R_delta = R_now @ R_baked.T

## Cross-BVH Checkpoint Sharing
- Added fallback: strip number+subject suffix from BVH stem (dance1_subject1 → dance) to find base checkpoint
- Allows dance checkpoint to work for all dance variant BVH files

## Walk LowLeg Baking — Completed
- Baked L_LowLeg and R_LowLeg for walk.bvh on A6000 server via slurm
- Both completed in ~4 min each (132 frames)
- Synced results locally — walk now has all 4 regions (37 muscles)
- Ready for full 37-muscle walk V1Dec training

## Architecture Diagram
- Generated V1Dec architecture diagram (volume_distill/v1dec_architecture.png)
- Detailed Q&A about network structure for slides

## Interactive Joint Position Editor
- Added joint edit mode to 3D viewer for adjusting DART skeleton joint positions
- Features: click-to-select (ray-cast picking), drag-to-move, XYZ numeric input, L/R symmetry toggle
- Save to XML button exports via `exportSkeleton()`, Reset All reverts to original
- Joint spheres drawn color-coded: green=selected, yellow=default
- Fixed multiple exportSkeleton crashes: missing `stretches`, `smpl_jidx` keys; safe temp-file write
- Fixed Import zygote_skel to also sync `new_skel_info`, `root_name`, `target_skel`

## Walk Rebake + Retrain (Updated Skeleton)
- Rebaked walk.bvh for all 4 regions (L/R UpLeg + L/R LowLeg) on A6000 server
- L/R vertex count mismatch in 12 muscles after skeleton change — handled by duplicating L data
- Trained V1Dec for 1000 epochs: MSE 3.13e-2 → 5.11e-7
- Checkpoint: `volume_distill/walk_mirror_checkpoints/best_v1dec.pt`
