# Research Journal — 2026-02-20

## Work Done
- **ARAP Solver Optimization**: Major performance push on the Taichi ARAP backend -- eliminated redundant GPU transfers, fused kernels, CSR gather pattern, GPU PCG solver (later reverted to scipy), warm-starting from previous frame
- **Reduced System Solver**: Replaced full-system factorized() with reduced free-DOF splu, iterated through splu/UMFPACK/factorized before settling on splu
- **Topology Caching**: Cached topology data in unified volume sim to avoid per-frame rebuild, separated field allocation from data upload
- **Bake Summary**: Added muscle count, parameter count, per-muscle timing, and frame count to bake summary

## Key Decisions
- Reverted GPU PCG solver back to scipy direct solve -- faster at this problem size
- Settled on splu on the reduced (free-DOF) system as the optimal solver configuration
