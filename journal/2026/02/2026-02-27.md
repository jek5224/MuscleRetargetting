# Research Journal — 2026-02-27

## Work Done
- **Neural Network Distillation**: Built complete muscle tet sim distillation pipeline -- training with TensorBoard logging, real-time NN inference toggle, positional encoding, per-muscle SIREN decoders (later replaced with LeakyReLU residual blocks)
- **Architecture Iteration**: Tried MLP -> SIREN residual blocks -> per-muscle SIREN decoders -> shared vertex decoder with learned embeddings -> LeakyReLU residual blocks, with gradient accumulation for OOM
- **Reference Frame**: Experimented with pelvis vs femur bone reference frames, added/reverted ankle DOFs, settled on pelvis with bone attachment snapping
- **Skeleton Collision**: Added collision detection to unified volume sim through multiple iterations -- proximity, interleaved ARAP, pre-solve constraints, post-solve pass, and finally removed it
- **Motion & Caching**: Fixed BVH parser for different joint naming conventions, added per-axis Fix X/Y/Z checkboxes, flush bake data to disk periodically, combined dance+locomotion training

## Key Decisions
- Replaced SIREN with LeakyReLU residual blocks after SIREN caused divergence
- Removed collision handling entirely from unified volume sim after many iterations showed no satisfactory approach
- Used pelvis reference frame (not femur) for NN, with weighted loss on anchor vertices instead of post-hoc snapping
