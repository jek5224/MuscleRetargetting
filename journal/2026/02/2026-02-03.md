# Research Journal — 2026-02-03

## Work Done
- **Motion Browser**: Added BVH motion playback panel with sequential stepping, repeat/loop, and draggable frame slider
- **Tet Deformation Baking**: Implemented baking pipeline -- per-frame tet simulation with cached playback, background thread baking (later switched to main-thread one-frame-per-render-loop for stability)
- **Waypoint System**: Added waypoint caching alongside tet positions, patch button for old cache files, and per-frame waypoint updates during playback
- **BVH Integration**: Added joint name mapping to zygote skeleton, fixed root translation pinning (only X/Z, let Y follow motion)
- **Performance**: Disabled verbose prints and draw arrays during bake mode, removed per-iteration waypoint updates

## Key Decisions
- Switched from threaded bake to main-thread one-frame-per-render-loop to keep UI responsive without thread-safety issues
- Cache waypoints alongside tet positions to avoid expensive per-frame recomputation
